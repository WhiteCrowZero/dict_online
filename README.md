# Dict Online

*Simple C/S Model Example*

This is a simple Client/Server (C/S) model example written in Python. The code demonstrates basic communication between a server and a client using Python’s `socket` library. It is created for educational purposes and can be used as a basic exercise to understand network communication.

## Description

- **Server** listens for incoming connections from the **Client**.
- **Client** sends a message to the **Server**.
- **Server** responds with a predefined message.
- Both scripts are designed to run on the same machine (localhost).
